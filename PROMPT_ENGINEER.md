# 提示词使用指南（可直接照抄）

本系统会把你的问题大致分成三类处理：
1) **法条原文/章节/全文（结构化直通 SQL）**：最准确，不走向量检索  
2) **法名 + 关键词（结构化关键词检索 SQL LIKE）**：在某一部法律内找“包含某词”的条文  
3) **法律分析/适用/怎么办（RAG 向量检索）**：输出“法律依据 → 法律分析与建议”，必要时会追问（同一 session 最多 2 轮）

---

## 1) 查法条原文（最稳、最推荐）

**模板（任选其一照抄）：**
- `请给出《<法律名称>》第<一/1>编 第<一/1>章 第<一/1>条的原文`
- `请给出《<法律名称>》第<一/1>章全部内容`
- `请给出《<法律名称>》全文`

**示例：**
- `请给出《中华人民共和国民法典》第一编 第一章 第一条的原文`
- `请给出《中华人民共和国刑法》第一编 第一章的原文`
- `请给出《劳动合同法》第七章全部内容`

**说明：**
- 命中后系统会直接查 MySQL 的 `law_text`，不会走向量检索。
- 数据库没收录就会直接提示“未找到匹配条文”，不会用相近条文糊弄。
- `第一条` / `第1条` 两种写法都可以（系统会做转换并兼容匹配）。

---

## 2) 在某一部法律里“按关键词”找条文

**模板：**
- `《<法律名称>》关于<关键词>的法条有哪些`
- `《<法律名称>》有关<关键词>的规定`

**示例：**
- `《中华人民共和国民法典》关于人权的法条有哪些`
- `《劳动合同法》关于试用期的规定`

**说明：**
- 这是 **在指定法律内做关键词匹配**（SQL LIKE），默认最多返回 10 条命中结果。
- 想更精确可把关键词缩短到核心词（例如“人权”“试用期”“解除合同”）。

---

## 3) 要法律分析/适用/方案（RAG）

**推荐写法（一次性给足信息，减少追问）：**
```
问题：<你要问的法律问题>
已知事实：
1) <谁与谁、关系>
2) <时间、地点、金额>
3) <关键证据：合同/聊天记录/转账凭证等>
我希望得到：<你想要的结论或行动建议>
```

**示例：**
```
问题：房东提前解约租赁合同，承租人能否要求赔偿？
已知事实：
1) 我与房东签了1年租赁合同
2) 还剩6个月，房东要求我一周内搬走
3) 有合同与微信聊天记录
我希望得到：能否要求违约金/赔偿，怎么操作
```

**输出特点：**
- 系统会输出两段：`法律依据` → `法律分析与建议`
- 若命中“是否构成/是否成立/责任/赔偿”等意图，且事实不足：
  - 会先给出【阶段性结论/风险点】（依据有限时会明确说明）
  - 再追问 1–3 个具体问题（同一 session 最多 2 轮）

---

## 3.1) 追问时怎么回答（让系统不重复问）

当系统追问 1–3 个问题时，你可以按以下方式回答（任选其一）：
- **按编号逐条回答**（最稳）：
  - `1) 16周岁  2) 1000元  3) 父母口头同意，有聊天记录`
- **用逗号/换行分隔**：
  - `16周岁，1000元，父母口头同意，有聊天记录`

说明：
- 如果你已执行 `sql/mysql_session_fact_schema.sql` 创建了 `session_fact`，系统会把你的回答沉淀成“已确认事实”，后续尽量不再重复追问。

## 4) 不想被追问？（可选）

在问题末尾加一句即可：
- `不要追问，直接给结论`
- `直接回答`

说明：这会让系统尽量在事实不完整时也先给出更保守的答复，并可能提示“依据有限”。

---

## 4.1) 想要更可执行的清单（证据/流程/风险点）

你可以在问题末尾加一句触发更偏“法律助理”的输出：
- `请给我证据清单`
- `请给我起诉/仲裁流程清单`
- `请列出主要风险点`

## 5) 常见排查（你问了但总是“找不到条文”）

如果你在查原文时收到：
`未在数据库找到匹配的条文，请确认法律名称或编号是否正确。`

请按顺序排查：
1) 先确认 `law_text` 里确实有这部法（`law_code` / `law_title` 中包含你的法律名称关键词）
2) 再确认条号存储形式（有的库是“第一条”，有的是“第1条”）：你两种都可以问，但前提是数据本身存在
3) 尝试把法律名称写全一些（例如用 `《中华人民共和国民法典》` 替换 `民法典`），以适配你库里的命名
4) 如果你要的是原文，请确保问题里明确写了 `原文/全文/全部内容` 或者包含 `第X条/第X章/第X编`
